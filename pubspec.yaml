name: fic_bloc_re_learn
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.0.2 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  flutter_bloc: ^8.1.3
  dartz: ^0.10.1
  http: ^1.0.0
  equatable: ^2.0.5
  shared_preferences: ^2.1.2
  google_fonts: ^5.0.0
  
dev_dependencies:
  build_runner: ^2.4.5
  flutter_test:
    sdk: flutter
  freezed: ^2.3.5

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
